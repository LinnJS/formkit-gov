{
  "name": "@formkit-gov/landing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp -r public/* dist/",
    "dev": "pnpm build && npx http-server dist -p 4000",
    "dev:open": "open http://localhost:4000 && pnpm build && npx http-server dist -p 4000",
    "lint": "echo 'Static HTML - no linting required'",
    "lint:fix": "echo 'Static HTML - no linting required'",
    "preview": "npx http-server dist -p 4000",
    "typecheck": "echo 'Static HTML - no type checking required'"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
